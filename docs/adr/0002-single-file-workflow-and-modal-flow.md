# Single-File Workflow and Initial Target Size Modal

To maximize clarity and ease of use, the File Size Increaser focuses on a single active file workflow.

Upon first uploading a file, an autofocus modal immediately prompts the user for their exact target size with preset shortcuts. Once submitted, the modal dismisses and transitions to the main interactive workspace where users can make live, instant size updates using an inline input field and download immediately.
